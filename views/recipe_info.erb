<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= @recipe_information[0]["name"] %></title>
  </head>

  <body>
    <h1><%= @recipe_information[0]["name"] %></h1>

    <p><%= @recipe_information[0]["description"] %></p>

    <h2>Ingredients:</h2>

    <ul>
      <% @recipe_information.each do |recipe| %>
        <li>
          <%= recipe["ingredients"] %>
        </li>
      <% end %>
    </ul>

    <h2> Directions: </h2>
    <p><%= @recipe_information[0]["instructions"] %></p>
  </body>
</html>
